{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/scanner",
            "args": ["test.cmm"],
            "cwd": "${workspaceFolder}"
        }
    ]
}