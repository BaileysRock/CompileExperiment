{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}/Code/Parser",
            "args": ["test.cmm"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build"
        }
    ]
}